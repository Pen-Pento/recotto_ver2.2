<div class="main posts-index">
  <div class="container">
    <% @spots.each do |spot| %>
      <div class="posts-index-item">
        <!-- post.contentを使って書き換えてください -->
        <%= link_to(spot.spot_one) %>
        <% spot.url_one %>
        
        <%= spot.spot_two %>
        <% spot.url_two %>
        
        <%= spot.spot_three %>
        <% spot.url_three %>
        
        <%= spot.spot_four %>
        <% spot.url_four %>
      </div>
    <% end %>
